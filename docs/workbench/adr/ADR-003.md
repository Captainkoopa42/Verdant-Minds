# ADR-003: Engine Process Isolation

**Status:** Accepted for Workbench architecture baseline

## Decision

Each active organism runs in a dedicated supervised Python worker process, outside the API server request thread.

## Consequence

Implementations that violate this boundary are treated as integration defects, not alternative UI behavior.
