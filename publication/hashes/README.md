# Immutable hash records

`SHA256SUMS.txt` is generated only after the Phase 0 files and preflight record are complete. Later phases append versioned hash manifests rather than overwriting historical manifests.

Hash records cover candidate identity, dependency locks, protocols, manifests, raw records, processed results, tables, figures, and reproduction packages.

