[project]
name = "verdant-workbench-backend"
version = "1.0.1"
requires-python = ">=3.11"
dependencies = ["fastapi==0.128.2", "pydantic==2.13.4", "uvicorn==0.48.0"]

[project.optional-dependencies]
test = ["pytest==9.0.2", "httpx==0.28.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
